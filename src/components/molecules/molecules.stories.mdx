import { Meta, Story } from '@storybook/addon-docs/blocks';

import DateInfo from './DateInfo';
import PageTitle from './PageTitle';
import ShareButtons from './ShareButtons';

<Meta title="molecules" />

# DateInfo

export const DateInfoTemplate = (args) => <DateInfo {...args} />;

<Canvas>
  <Story
    name="DateInfo"
    args={{
      publishedAt: new Date(),
      modifiedAt: new Date(),
    }}
  >
    {DateInfoTemplate.bind({})}
  </Story>
</Canvas>

# PageTitle

export const PageTitleTemplate = (args) => <PageTitle {...args} />;

<Canvas>
  <Story
    name="PageTitle"
    args={{
      title: 'Title',
    }}
  >
    {PageTitleTemplate.bind({})}
  </Story>
</Canvas>

# ShareButtons

export const ShareButtonsTemplate = (args) => <ShareButtons {...args} />;

<Canvas>
  <Story
    name="ShareButtons"
    args={{
      url: 'https://blog.abekoh.dev/posts/build-blog-with-nextjs',
      title: 'Next.js+Vercel+microCMSでブログをリニューアルしました',
    }}
  >
    {ShareButtonsTemplate.bind({})}
  </Story>
</Canvas>
